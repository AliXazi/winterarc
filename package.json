{
  "name": "winterarc",
  "version": "1.4.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "dev": "npm run build && wrangler pages dev public --env-file .env.local",
    "deploy": "npm run build && wrangler pages deploy public",
    "test": "node tests/test_cloudflare.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1",
    "@neondatabase/serverless": "^1.1.0",
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  }
}
